version: 1.0.{build}
image: Visual Studio 2015

before_build:
- cmd: >-
    dotnet.exe restore

build:
  publish_nuget: false
  publish_nuget_symbols: false
  include_nuget_references: false
  verbosity: minimal